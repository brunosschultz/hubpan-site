# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Fotos originais do cliente (brutas, alta resolução) — não versionar, só os webp otimizados em public/images
assets-cliente

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variáveis de ambiente locais (chaves do Supabase) — nunca versionar
.env
.env.local

# Cache local do Supabase CLI (link do projeto, versões) — não versionar
supabase/.temp
.vercel
.env*
